
符号创建法 [] , (), {}


1、数组
数组其实不是“一种”复合数据类型，而是“无穷多”种数据类型 ，比如 [i32; 5] 与 [i32; 6] 不是同一种数据类型


2、tuple 与 struct 
tuple是具体值的集合，tuple是具体值，一次性的

- 数组与tuple只是值的一次性组合，不具有生产性或者重复性。

struct是数据类型的集合，struct能产生这个结构的具体数值


3、创建一个struct 对象的两种方法
（1）  Struct_Name { }
（2）  Struct_Name::new()


4、 tuple struct
struct Color(i32, i32, i32);
struct Point(i32, i32, i32);
fn main() {
    let black = Color(0, 0, 0);
    let origin = Point(0, 0, 0);
}



